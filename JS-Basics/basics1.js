// to print something in the output 
console.log('Hola Emiliano');

// In JavaScript ther is no Data type definition for the variables
// From ES6 we have three different ways to declare a variable var, let, const

// what is the difference between var, let and const?

let a = 4;
console.log(a);

// in order to know the data type, we can use the typeof()

console.log(typeof(a)); // number

// string variables
let c = 'Emiliano Duque P';
console.log(c);
console.log(typeof(c));

// boolean
let required = true;
console.log(typeof(required));

let b = 8;
let d = a + b;
console.log(d);

// Note: difference between let and var is that let does not let you re-declare a varible using 'let', but if you use 'var' it is possible.

// negation operatorn !

console.log(required);
console.log(!required);
console.log(!!required);
